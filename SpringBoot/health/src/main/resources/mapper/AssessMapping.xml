<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.work.health.dao.AssessDao">
    <insert id="insertAssess" parameterType="com.work.health.bean.Assess">
        INSERT assess
        (assess,createName,createTime)
        VALUES
        (#{assess},#{createName},current_time)
    </insert>

    <update id="updateUserAssess" parameterType="String">
        UPDATE tb_user
        SET isAssess = 1
        WHERE id = #{workId}
    </update>

    <update id="updateWorkerAssess" parameterType="String">
        UPDATE tb_worker
        SET isAssess = 1
        WHERE id = #{workId}
    </update>

    <select id="getCount" resultType="com.work.health.bean.Assess">
        SELECT (SELECT COUNT(*) FROM assess WHERE assess = 1) AS veryPoor,(SELECT COUNT(*) FROM assess WHERE assess = 2) AS disappointed
        ,(SELECT COUNT(*) FROM assess WHERE assess = 3) AS generally,(SELECT COUNT(*) FROM assess WHERE assess = 4) AS satisfy
        ,(SELECT COUNT(*) FROM assess WHERE assess = 5) AS surprise
    </select>
</mapper>